Curated MIDI fixtures live here.

Commit small, real `.mid` captures (from the piano via M0 recording) so remote
and cloud agents can develop and verify scoring against real performance data
without the physical instrument. Keep them tiny and representative.
