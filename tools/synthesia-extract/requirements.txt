# Synthesia visual extractor (M6-C) — runtime + test dependencies.
# Headless OpenCV keeps this usable in CI / cloud sandboxes (no GUI libs).
numpy>=1.24
opencv-python-headless>=4.8

# Test-only:
pytest>=7.0
