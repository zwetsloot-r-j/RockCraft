# Synthesia visual extractor (M6-C) + audio fusion (M6-F) — runtime + test deps.
# Headless OpenCV keeps this usable in CI / cloud sandboxes (no GUI libs).
# Audio fusion's transcriber and WAV I/O use only numpy + the stdlib `wave`,
# so no audio dependency is added here.
numpy>=1.24
opencv-python-headless>=4.8

# Optional (NOT installed by default): a learned piano-transcription model can
# replace the bundled DSP transcriber for real-world accuracy — see the README.
#   basic-pitch>=0.3   # heavy TF/Torch deps; fusion works without it.

# Test-only:
pytest>=7.0
