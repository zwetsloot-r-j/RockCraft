//! RockCraft terminal frontend (MVP).
//!
//! This is the swappable view layer — the same `core` engine will later drive a
//! Tauri desktop app and a Godot visual frontend. At M0 ("Echo") this becomes a
//! `ratatui` app that lists live MIDI events from the piano and records them to
//! a `.mid` file.

fn main() {
    println!("RockCraft — scaffold OK. M0 'Echo' goes here.");
}
