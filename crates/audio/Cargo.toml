[package]
name = "rockcraft-audio"
description = "Audio playback, metronome, and MIDI synthesis. Wraps cpal/rodio/rustysynth (later)."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
rockcraft-core.workspace = true
# Audio output + decoding + mixing (wraps cpal). rustysynth: pure-Rust
# SoundFont synthesizer for turning MIDI notes into piano sound.
rodio = "0.20"
rustysynth = "1.3"
